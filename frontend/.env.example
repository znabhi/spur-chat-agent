# URL used by the browser to call the API (injected into bundle via import.meta.env)
VITE_API_URL=http://localhost:3001

# URL used by vite.config.ts proxy at dev-server startup (plain Node env var)
BACKEND_URL=http://localhost:3001
